spring:
  datasource:
    url: jdbc:mysql://localhost:3306/AngularSpringBoot
    username: root
    password: TempDump989
    driverClassName: com.mysql.cj.jdbc.Driver
    initialize: false
  jpa:
    database-platform: org.hibernate.dialect.MySQL57InnoDBDialect
    hibernate:
      ddl-auto: update